apiVersion: rabbitmq.com/v1beta1
kind: Exchange
metadata:
  name: virtual-collection-ingestion-exchange-dlq
  namespace: rabbitmq
spec:
  name: virtual-collection-ingestion-exchange-dlq # name of the exchange
  autoDelete: false
  durable: true
  rabbitmqClusterReference:
    name: rabbitmq-cluster # name of the RabbitMQ cluster
---
apiVersion: rabbitmq.com/v1beta1
kind: Binding
metadata:
  name: virtual-collection-ingestion-binding-dlq
  namespace: rabbitmq
spec:
  source: virtual-collection-ingestion-exchange-dlq # an existing exchange
  destination: virtual-collection-ingestion-queue-dlq # an existing queue
  destinationType: queue # can be 'queue' or 'exchange'
  routingKey: "virtual-collection-ingestion-dlq"
  rabbitmqClusterReference:
    name: rabbitmq-cluster # name of the RabbitMQ cluster
---
apiVersion: rabbitmq.com/v1beta1
kind: Queue
metadata:
  name: virtual-collection-ingestion-queue-dlq # name of this custom resource; does not have to the same as the actual queue name
  namespace: rabbitmq
spec:
  name: virtual-collection-ingestion-queue-dlq # name of the queue
  autoDelete: false
  durable: true
  type: "quorum"
  rabbitmqClusterReference:
    name: rabbitmq-cluster # name of the RabbitMQ cluster