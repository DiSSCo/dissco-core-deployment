apiVersion: rabbitmq.com/v1beta1
kind: Exchange
metadata:
  name: virtual-collection-exchange-dlq
  namespace: rabbitmq
spec:
  name: virtual-collection-exchange-dlq # name of the exchange
  autoDelete: false
  durable: true
  rabbitmqClusterReference:
    name: rabbitmq-cluster # name of the RabbitMQ cluster
---
apiVersion: rabbitmq.com/v1beta1
kind: Binding
metadata:
  name: virtual-collection-binding-dlq
  namespace: rabbitmq
spec:
  source: virtual-collection-exchange-dlq # an existing exchange
  destination: virtual-collection-queue-dlq # an existing queue
  destinationType: queue # can be 'queue' or 'exchange'
  routingKey: "virtual-collection-dlq"
  rabbitmqClusterReference:
    name: rabbitmq-cluster # name of the RabbitMQ cluster
---
apiVersion: rabbitmq.com/v1beta1
kind: Queue
metadata:
  name: virtual-collection-queue-dlq # name of this custom resource; does not have to the same as the actual queue name
  namespace: rabbitmq
spec:
  name: virtual-collection-queue-dlq # name of the queue
  autoDelete: false
  durable: true
  type: "quorum"
  rabbitmqClusterReference:
    name: rabbitmq-cluster # name of the RabbitMQ cluster