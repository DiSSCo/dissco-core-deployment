apiVersion: rabbitmq.com/v1beta1
kind: Exchange
metadata:
  name: mas-scheduler-exchange-dlq
  namespace: rabbitmq
spec:
  name: mas-scheduler-exchange-dlq # name of the exchange
  autoDelete: false
  durable: true
  rabbitmqClusterReference:
    name: rabbitmq-cluster # name of the RabbitMQ cluster
---
apiVersion: rabbitmq.com/v1beta1
kind: Binding
metadata:
  name: mas-scheduler-binding-dlq
  namespace: rabbitmq
spec:
  source: mas-scheduler-exchange-dlq # an existing exchange
  destination: mas-scheduler-queue-dlq # an existing queue
  destinationType: queue # can be 'queue' or 'exchange'
  routingKey: "mas-scheduler-dlq"
  rabbitmqClusterReference:
    name: rabbitmq-cluster # name of the RabbitMQ cluster
---
apiVersion: rabbitmq.com/v1beta1
kind: Queue
metadata:
  name: mas-scheduler-queue-dlq # name of this custom resource; does not have to the same as the actual queue name
  namespace: rabbitmq
spec:
  name: mas-scheduler-queue-dlq # name of the queue
  autoDelete: false
  durable: true
  type: "quorum"
  rabbitmqClusterReference:
    name: rabbitmq-cluster # name of the RabbitMQ cluster