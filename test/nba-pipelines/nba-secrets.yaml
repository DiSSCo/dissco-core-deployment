apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: nba-secrets
  namespace: airflow
spec:
  provider: aws
  secretObjects:
    - secretName: nba-secrets
      type: Opaque
      data:
        - objectName: elastic-username
          key: elastic-username
        - objectName: elastic-password
          key: elastic-password
        - objectName: minio-access-key
          key: minio-access-key
        - objectName: minio-access-key-secret
          key: minio-access-key-secret
        - objectName: s3-access-key
          key: s3-access-key
        - objectName: s3-access-key-secret
          key: s3-access-key-secret
  parameters:
    objects: |
      - objectName: "arn:aws:secretsmanager:eu-west-2:824841205322:secret:nba-test-secrets-K6O2y2"
        jmesPath:
            - path: "elastic_username"
              objectAlias: "elastic-username"
            - path: "elastic_password"
              objectAlias: "elastic-password"
            - path: "minio_access_key"
              objectAlias: "minio-access-key"
            - path: "minio_access_key_secret"
              objectAlias: "minio-access-key-secret"
            - path: "s3_access_key"
              objectAlias: "s3-access-key"
            - path: "s3_access_key_secret"
              objectAlias: "s3-access-key-secret"